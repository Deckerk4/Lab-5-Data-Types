{"dateFashion-code":"function dateFashion(you, date) {\n  // Check if either of you has style of 2 or less\n  if (you <= 2 || date <= 2) {\n    return 0; // Result is 0 (no)\n  } else if (you >= 8 || date >= 8) {\n    return 2; // Result is 2 (yes)\n  } else {\n    return 1; // Result is 1 (maybe)\n  }\n}","countXX":"true","parrotTrouble":"true","doubleX":"true","stringTimes-code":"function stringTimes(str, n) {\n  // Using the repeat method to create a larger string with n copies of the original string\n  return str.repeat(n);\n}\n","squirrelPlay":"true","doubleX-code":"function doubleX(str) {\n  // Find the index of the first occurrence of 'x'\n  const firstXIndex = str.indexOf('x');\n\n  // Check if the next character is also 'x'\n  return firstXIndex !== -1 && str.charAt(firstXIndex + 1) === 'x';\n}","parrotTrouble-code":"function parrotTrouble(talking, hour){\n  return talking && (hour < 7 || hour > 20);\n}","helloName":"true","monkeyTrouble-code":"function monkeyTrouble(aSmile, bSmile){\n  return (aSmile && bSmile) || (!aSmile && !bSmile);\n}","cigarParty-code":"function cigarParty(cigars, isWeekend) {\n  // Check if it's the weekend\n  if (isWeekend) {\n    return cigars >= 40; // Successful party if there are 40 or more cigars\n  } else {\n    return cigars >= 40 && cigars <= 60; // Successful party if there are between 40 and 60 cigars (inclusive)\n  }\n}","frontTimes-code":"function frontTimes(str, n) {\n  // Extract the front of the string (first 3 chars or the whole string if length is less than 3)\n  const front = str.length < 3 ? str : str.substring(0, 3);\n  \n  // Using the repeat method to create a larger string with n copies of the front\n  return front.repeat(n);\n}","notString-code":"function notString(str){\n  if (str.startsWith(\"not\")) {\n    return str;\n  } else {\n     return \"not \" + str;\n  }\n}","nearHundred":"true","makes10-code":"function makes10(a, b){\n  return a === 10 || b === 10 || a + b === 10;\n}","makeOutWord-code":"function makeOutWord(out, word) {\n  return out.substring(0, 2) + word + out.substring(2, 4);\n}","makeTags-code":"function makeTags(tag, word) {\n  return '<' + tag + '>' + word + '</' + tag + '>';\n}","commonEnd":"true","makeOutWord":"true","sumDouble":"true","makes10":"true","caughtSpeeding-code":"function caughtSpeeding(speed, isBirthday) {\n  // Adjust the speed limit based on whether it's your birthday\n  const birthdaySpeedLimit = isBirthday ? 5 : 0;\n\n  // Check the speed and compute the result\n  if (speed <= 60 + birthdaySpeedLimit) {\n    return 0; // No ticket\n  } else if (speed <= 80 + birthdaySpeedLimit) {\n    return 1; // Small ticket\n  } else {\n    return 2; // Big ticket\n  }\n}","makePi-code":"function makePi() {\n  // Return an array containing the first 3 digits of pi\n  return [3, 1, 4];\n}\n","nearHundred-code":"function nearHundred(n){\n  return Math.abs(100 - n) <= 10 || Math.abs(200 - n) <= 10;\n\n}","makePi":"true","makeAbba":"true","makeAbba-code":"function makeAbba(a, b) {\n  return a + b + b + a;\n}\n","helloName-code":"function helloName(name) {\n  return 'Hello ' + name + '!';\n}\n","caughtSpeeding":"true","posNeg-code":"function posNeg(a, b, negative){\n  return (negative) ? (a < 0 && b < 0) : ((a < 0 && b > 0) || (a > 0 && b < 0));\n}","dateFashion":"true","firstLast6":"true","missingChar-code":"function missingChar(str, n){\n   return str.slice(0, n) + str.slice(n + 1);\n}","countXX-code":"function countXX(str) {\n  let count = 0;\n\n  // Loop through the string, checking for 'xx' at each position\n  for (let i = 0; i < str.length - 1; i++) {\n    if (str.substring(i, i + 2) === 'xx') {\n      count++;\n    }\n  }\n\n  return count;\n}","sameFirstLast-code":"function sameFirstLast(nums) {\n  // Check if the array is length 1 or more, and the first and last elements are equal\n  return nums.length >= 1 && nums[0] === nums[nums.length - 1];\n}","sleepIn":"true","debug":"honey:core-sdk:*","sleepIn-code":"function sleepIn(weekday, vacation){\nreturn !weekday || vacation; \n}","missingChar":"true","squirrelPlay-code":"function squirrelPlay(temp, isSummer) {\n  // Check if it's summer and adjust the upper temperature limit accordingly\n  const upperLimit = isSummer ? 100 : 90;\n\n  // Check if the temperature is between 60 and the adjusted upper limit (inclusive)\n  return temp >= 60 && temp <= upperLimit;\n}","makeTags":"true","notString":"true","stringBits-code":"function stringBits(str) {\n  let result = '';\n\n  // Loop through the string, taking every other character\n  for (let i = 0; i < str.length; i += 2) {\n    result += str.charAt(i);\n  }\n\n  return result;\n}","monkeyTrouble":"true","commonEnd-code":"function commonEnd(a, b) {\n  // Check if they have the same first element or the same last element\n  return a[0] === b[0] || a[a.length - 1] === b[b.length - 1];\n}\n","sameFirstLast":"true","diff21-code":"function diff21(n){\n  let absoluteDifference = Math.abs(n - 21);\n  return (n > 21) ? 2 * absoluteDifference : absoluteDifference;\n}","frontTimes":"true","cigarParty":"true","firstLast6-code":"function firstLast6(nums) {\n  // Check if 6 appears as either the first or last element in the array\n  return nums[0] === 6 || nums[nums.length - 1] === 6;\n}","stringBits":"true","stringTimes":"true","sumDouble-code":"function sumDouble(a, b){\n  return (a === b) ? 2 * (a + b) : (a + b);\n}","posNeg":"true","diff21":"true"}
